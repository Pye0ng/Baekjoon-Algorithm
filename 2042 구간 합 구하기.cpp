#include<stdio.h>
#include<vector>
using namespace std;
vector<ll> s;

	int m=(a+b)/2;
}

	int m=(a+b)/2;
}

	if(a!=b){
		int m=(a+b)/2;
	}
}

int main(){
	scanf("%d %d %d", &n, &m, &k);
		scanf("%d %d %lld", &a, &b, &c);
		}
	}
}
